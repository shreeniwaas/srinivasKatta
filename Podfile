# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Fix-Fixo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
 pod 'Alamofire', '~> 5.6'
  pod 'SwiftyJSON'
 pod 'WMSegmentControl'
  # Pods for Fix-Fixo

  target 'Fix-FixoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Fix-FixoUITests' do
    # Pods for testing
  end

end
